

        <p>current session["user"] is:
        <%= session["user"] != nil ? escape_html( session["user"] ) : "nil"  %></p>

        <p> All current entries in the User database:</p>

        <% User.all.each do |entry| %>
            <%= "<p>" + escape_html(entry.username) + "</p>" %>
            <%= "<p>" + entry.created_at.to_s + "</p>" %>
        <% end %>

